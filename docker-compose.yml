version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
  bot:
    build:
      context: ../sonarftbot
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

